import 'package:flutter/material.dart';

class ReleaseTimelineFormField extends StatelessWidget {
  const ReleaseTimelineFormField({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('what is your release timeline?');
  }
}
