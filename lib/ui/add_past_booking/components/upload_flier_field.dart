import 'package:flutter/material.dart';

class UploadFlierField extends StatelessWidget {
  const UploadFlierField({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('upload a poster/flier (optional');
  }
}
