import 'package:flutter/material.dart';

class SocialFollowingMenu extends StatelessWidget {
  const SocialFollowingMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: double.infinity,
      color: Colors.red,
    );
  }
}
