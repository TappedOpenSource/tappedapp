import 'package:flutter/material.dart';

class PastBookingsSlider extends StatelessWidget {
  const PastBookingsSlider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: double.infinity,
      color: Colors.red,
    );
  }
}
