import 'package:flutter/material.dart';

class UnsubscribedView extends StatelessWidget {
  const UnsubscribedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('no record label'),
    );
  }
}
